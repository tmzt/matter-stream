[package]
name = "matterstream-ui-cpu-raster"
version = "0.1.0"
edition = "2021"
description = "CPU software rasterizer — evaluates SDF draw commands into an RGBA framebuffer"
license = "Apache-2.0"

[dependencies]
matterstream-common = { path = "../matterstream-common" }
