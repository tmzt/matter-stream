//! Re-export UI VM types from matterstream-vm.
//!
//! The canonical implementation lives in `matterstream_vm::ui_vm`.
//! CPU-side rendering has moved to `matterstream-ui`.

pub use matterstream_vm::ui_vm::*;
